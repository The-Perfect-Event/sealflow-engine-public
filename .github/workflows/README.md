# Workflows

Intentionally empty. Upstream Documenso workflows were removed at fork time —
they were tied to upstream's CI infrastructure (Crowdin token, npm publish,
deploy targets, issue labelers, etc.) and do not apply to our owned fork.

Our own CI lands in Phase 1c of sealflow#13: source-build → GHCR push.
